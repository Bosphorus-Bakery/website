// '^' amd '$' marks start and end of string while '+' matches preceding pattern 1 or more times
export const namePattern: RegExp = /^[a-zA-Z]+$/;

// TO DO: Regex patterns for name, email, phone, subj, description