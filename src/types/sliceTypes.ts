export interface AppState {
  isDark: boolean;
  theme: boolean;
}

export interface ContactFormState {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  subject: string;
  description: string;
}